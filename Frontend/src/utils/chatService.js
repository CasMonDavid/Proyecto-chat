export const joinChat = (roomName) => {
  console.log(`Uniéndose al chat: ${roomName}`);
  // Aquí iría la lógica real de conexión al chat LAN
};

export const createChat = () => {
  console.log('Creando nuevo chat');
  // Aquí iría la lógica real de creación de chat LAN
};